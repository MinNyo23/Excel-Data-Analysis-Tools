// Supabase authentication is intentionally disabled for the Vercel-hosted app.
// Authentication is handled by the application’s Vercel/Manus OAuth session.
export const supabase = null;
export const usesSupabaseAuth = false;
